<Window x:Class="lab4_5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:lab4_5.Controls"
        xmlns:conv="clr-namespace:lab4_5.Converters"
        xmlns:cmd="clr-namespace:lab4_5.Commands"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitle}"
        MinWidth="880"
        MinHeight="520"
        Height="680"
        Width="1180"
        Icon="pack://application:,,,/Assets/app.ico">
    <Window.Resources>
        <conv:UriToImageConverter x:Key="UriToImg"/>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="OnCloseExecuted"/>
        <CommandBinding Command="{x:Static cmd:ShopCommands.ShowRoutingInfo}" Executed="OnRoutingInfoExecuted"/>
    </Window.CommandBindings>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{DynamicResource MenuLang}">
                <MenuItem Header="{DynamicResource MenuLangRu}" Command="{Binding SetLangRuCommand}"/>
                <MenuItem Header="{DynamicResource MenuLangEn}" Command="{Binding SetLangEnCommand}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource MenuTheme}">
                <MenuItem Header="{DynamicResource ThemeOptimistic}" Command="{Binding SetThemeCommand}" CommandParameter="optimistic"/>
                <MenuItem Header="{DynamicResource ThemePink}" Command="{Binding SetThemeCommand}" CommandParameter="pink"/>
                <MenuItem Header="{DynamicResource ThemeGray}" Command="{Binding SetThemeCommand}" CommandParameter="gray"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource MenuHelp}">
                <MenuItem Header="{DynamicResource MenuRoutingInfo}" Command="{x:Static cmd:ShopCommands.ShowRoutingInfo}" InputGestureText="F1"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource ProfileTitle}" Command="{Binding OpenProfileCommand}"/>
        </Menu>

        <ToolBar DockPanel.Dock="Top">
            <Label Content="{DynamicResource RoleLabel}" VerticalAlignment="Center"/>
            <ComboBox Width="160" SelectedIndex="{Binding RoleIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ComboBoxItem Content="{DynamicResource RoleClient}"/>
                <ComboBoxItem Content="{DynamicResource RoleAdmin}"/>
            </ComboBox>
            <Separator/>
            <Button Content="{DynamicResource BtnAdd}" Command="{Binding AddCommand}" />
            <Button Content="{DynamicResource BtnSave}" Command="{Binding EditSaveCommand}" Style="{StaticResource SaveActionButtonStyle}" />
            <Button Content="{DynamicResource BtnDelete}" Command="{Binding DeleteCommand}" />
            <Separator/>
            <Button Content="{DynamicResource BtnUndo}" Command="{Binding UndoCommand}"/>
            <Button Content="{DynamicResource BtnRedo}" Command="{Binding RedoCommand}"/>
            <Separator/>
            <Button Content="{DynamicResource BtnOpenCatalog}" Command="{Binding OpenCatalogCommand}"/>
            <Button Content="{DynamicResource BtnOpenCart}" Command="{Binding OpenCartCommand}"/>
        </ToolBar>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusSummary}"/>
            </StatusBarItem>
        </StatusBar>

        <TabControl SelectedIndex="{Binding ActivePageIndex, Mode=TwoWay}">
            <TabItem Header="{DynamicResource TabCatalog}">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="240"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Grid.Column="0" Header="{DynamicResource FilterHeader}" Margin="0,0,8,0">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="{DynamicResource BtnApply}" Command="{Binding ApplyFilterCommand}" />
                                <Button Content="{DynamicResource BtnClear}" Command="{Binding ClearFilterCommand}" />
                            </StackPanel>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <Label Content="{DynamicResource SearchLabel}"/>
                                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Label Content="{DynamicResource CategoryLabel}"/>
                                    <ComboBox ItemsSource="{Binding CategoryOptions}" SelectedItem="{Binding FilterCategory, Mode=TwoWay}"/>
                                    <Label Content="{DynamicResource PriceFrom}"/>
                                    <TextBox Text="{Binding PriceMinStr, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Label Content="{DynamicResource PriceTo}"/>
                                    <TextBox Text="{Binding PriceMaxStr, UpdateSourceTrigger=PropertyChanged}"/>
                                    <CheckBox Content="{DynamicResource OnlyInStock}" IsChecked="{Binding OnlyInStock, Mode=TwoWay}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </ScrollViewer>
                        </DockPanel>
                    </GroupBox>

                    <ListBox Grid.Column="1"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ItemsSource="{Binding FilteredProducts}"
                             SelectedItem="{Binding SelectedProduct, Mode=TwoWay}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="255"
                                        x:Name="ProductCardBorder"
                                        Margin="0,0,10,10"
                                        Padding="10"
                                        BorderBrush="{DynamicResource BrushGridBorder}"
                                        BorderThickness="1"
                                        Background="{DynamicResource BrushPanel}"
                                        CornerRadius="8"
                                        Loaded="OnProductCardLoaded"
                                        MouseLeftButtonUp="OnProductCardClick">
                                    <StackPanel>
                                        <Image Height="200"
                                            Width="255"
                                            Stretch="Uniform"
                                            Margin="0,0,0,8"
                                            Source="{Binding PrimaryImagePath, Converter={StaticResource UriToImg}}"/>
                                        <TextBlock Text="{Binding ShortName}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding Category}" Margin="0,3,0,0"/>
                                        <TextBlock Margin="0,8,0,0" FontWeight="SemiBold" Text="{Binding PriceWithDiscount, StringFormat={}{0:N2}}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="{DynamicResource ProductInStockLabel}"/>
                                            <TextBlock Text="{Binding Quantity}"/>
                                        </StackPanel>
                                        <ctrl:StockLevelBarControl Value="{Binding Quantity}" Maximum="100" BarHeight="12" Margin="0,6,0,0"/>
                                        <Button Content="{DynamicResource BtnAddToCart}"
                                                Margin="0,10,0,0"
                                                HorizontalAlignment="Left"
                                                Tag="{Binding}"
                                                Click="OnAddToCartClick"/>
                                        <Button Content="{DynamicResource BtnAddImageToCard}"
                                                Margin="0,6,0,0"
                                                HorizontalAlignment="Left"
                                                Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVis}}"
                                                Tag="{Binding}"
                                                Click="OnAddImageToProductClick"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <TabItem Header="{DynamicResource TabCart}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="0"
                              Margin="0,0,0,8"
                              ItemsSource="{Binding CartItems}"
                              SelectedItem="{Binding SelectedCartItem, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              RowHeight="36">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource CartColProduct}" Binding="{Binding Product.ShortName}" Width="*"/>
                            <DataGridTextColumn Header="{DynamicResource CartColPrice}" Binding="{Binding Product.PriceWithDiscount, StringFormat=N2}" Width="60"/>
                            <DataGridTemplateColumn Header="{DynamicResource CartColQty}" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ctrl:QuantityStepperControl Count="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                     MinCount="1"
                                                                     MaxCount="{Binding MaxQuantity}"
                                                                     Step="1"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{DynamicResource CartColTotal}" Binding="{Binding LineTotal, StringFormat=N2}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="1" Padding="10" BorderBrush="{DynamicResource BrushGridBorder}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource CartItemsCountLabel}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding CartItemsCount}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                                <TextBlock Text="{DynamicResource CartTotalLabel}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding CartTotal, StringFormat=N2}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <Label Content="{DynamicResource LblDeliveryAddress}"/>
                            <TextBox Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}"/>
                            <Label Content="{DynamicResource LblPaymentMethod}"/>
                            <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod, Mode=TwoWay}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Content="{DynamicResource BtnRemoveFromCart}" Command="{Binding RemoveFromCartCommand}"/>
                                <Button Content="{DynamicResource BtnCheckout}" Margin="10,0,0,0" Command="{Binding CheckoutCommand}" Style="{StaticResource SaveActionButtonStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
